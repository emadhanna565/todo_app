import 'package:flutter/material.dart';

class SettingsListTap extends StatelessWidget {
  const SettingsListTap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'settings tab',
      ),
    );
  }
}
